---
parent_label: proxy-cache  # This is shared with other '.proxy' subprojects, like RPM-Cache

container_home: "{{ ansible_env.HOME }}/.uni/container/{{ parent_label }}/apt-cacher-ng"

container_name: apt-cacher-ng.server

cache_data_dir: "{{ ansible_env.HOME }}/.uni/data/{{ parent_label }}/apt-cacher-ng"
